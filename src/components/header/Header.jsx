import Selected from "../selected/Selected";
import "./Header.css"
const Header = () => {
    return (
      <header className="container">
    <Selected/>
      </header>
    );
}

export default Header