import FilteredItems from "../filtered/filteredItems";
import "./Header.css"
const Header = () => {
    return (
      <header className="container">
    <FilteredItems/>
      </header>
    );
}

export default Header